{
  "config": {
    "step": {
      "user": {
        "title": "BLE Observer",
        "description": "Passively observes and classifies Bluetooth Low Energy devices detected by ble_monitor."
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "BLE Observer Options",
        "data": {
          "active_window_minutes": "Active window (minutes)",
          "retention_days": "Retention (days)",
          "mask_identifiers": "Mask MAC addresses in diagnostics",
          "include_timestamps": "Include timestamps in diagnostics",
          "promoted": "Promoted devices (list of objects)"
        }
      }
    }
  }
}
